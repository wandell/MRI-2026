# MRI-2026

Quarto repository for MRI tutorial slides and lecture notes (joint with Jon Winawer).

## Structure

- `notes/` for lecture notes (`.qmd`)
- `slides/` for slide decks (`.qmd`)
- `slides/images/` for images exported from PowerPoint decks
- `_quarto.yml` for shared HTML/PDF rendering defaults
- `.github/copilot-instructions.md` for AI authoring guidance
